# Slice Intersection

Implement a function that returns the intersection of two integer slices.  
The intersection must respect element frequency (multiset behavior).

```go
func Intersection(x, y []int) []int
```